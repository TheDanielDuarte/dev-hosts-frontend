import { SameTextValidatorDirective } from './same-text-validator.directive';

describe('SameTextValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SameTextValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
