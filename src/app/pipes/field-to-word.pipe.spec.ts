import { FieldToWordPipe } from './field-to-word.pipe';

describe('FieldToWordPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldToWordPipe();
    expect(pipe).toBeTruthy();
  });
});
